dist: focal

notifications:
  email:
    on_success: change
    on_failure: change

sudo: false
language: python
python:
  - "3.6"
  - "3.7"
  - "3.8"
  - "3.9-dev"

install:
  - pip install -U pip wheel
  - pip install -r requirements.txt

script:
  - tox
